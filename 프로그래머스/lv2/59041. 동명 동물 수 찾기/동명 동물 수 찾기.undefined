-- 코드를 입력하세요
SELECT distinct(N.NAME), N.COUNT  FROM ANIMAL_INS A
INNER JOIN (SELECT NAME, COUNT(NAME) AS COUNT FROM ANIMAL_INS
           GROUP by name
           ) N 
    ON A.NAME = N.NAME
WHERE N.COUNT >= 2
ORDER BY N.NAME